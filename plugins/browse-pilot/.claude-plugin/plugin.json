{
  "name": "browse-pilot",
  "description": "Cross-browser automation — control Firefox/Chrome/Edge via WebExtension API",
  "version": "0.1.4",
  "mcpServers": {
    "browse-pilot": {
      "command": "bp_cli",
      "args": ["--mcp", "--browser", "firefox", "--port", "9222", "--timeout", "60000"]
    }
  }
}
